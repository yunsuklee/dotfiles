-- Disabled plugins
-- Add plugins here with { "plugin/name", enabled = false } to disable them
-- This is useful for temporarily disabling plugins without removing their config

return {
  -- Example: disable a plugin
  -- { "plugin/name", enabled = false },
}